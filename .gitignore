# OS / editor junk
.DS_Store
**/.DS_Store
*:Zone.Identifier

# Build output (generated by `node build.mjs` locally and in CI)
/dist/

# Local tooling caches
/.wrangler/
node_modules/

# Secrets / local env (never commit API keys)
.env
.env.*
!.env.example
infrastructure/.env
infrastructure/.env.*
!infrastructure/.env.example

# Local dumps / scratch
/tmp/
